// Variant b, box-only print layout (open face up, floor on the bed, support-free).
// The trailing assignments override the defaults from the include.
include <photo_box_003.scad>
variant = "b";
part = "box";
