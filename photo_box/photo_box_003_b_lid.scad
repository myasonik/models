// Variant b, lid-only print layout (visible face down on the bed, skirt up, support-free).
// The trailing assignments override the defaults from the include.
include <photo_box_003.scad>
variant = "b";
part = "lid";
